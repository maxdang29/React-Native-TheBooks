import {put, takeLatest, call} from 'redux-saga/effects';
import * as ActionTypes from '../home/actions/typesAction';
import * as actions from '../home/actions/action';
import store from '../store';

const rootSagaHome = () => [];
export default rootSagaHome();
